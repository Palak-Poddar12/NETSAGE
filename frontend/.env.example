# NetSage AI Frontend Environment Configuration
# Leave empty (recommended) to use the Vite dev server proxy (/api -> http://127.0.0.1:8000).
# Set a full URL to talk to a backend running elsewhere, e.g.:
# VITE_API_URL=http://192.168.1.50:8000
VITE_API_URL=